<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Omino" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Arcade palette base -->
        <item name="android:windowBackground">@color/arcade_bg</item>
        <item name="android:textColor">@color/arcade_text</item>
        <item name="android:fontFamily">@font/press_start_2p</item>
        <item name="colorPrimary">@color/arcade_text</item>
        <item name="colorSecondary">@color/arcade_text</item>
        <!-- Status bar / nav bar -->
        <item name="android:statusBarColor">@color/arcade_bg</item>
        <item name="android:navigationBarColor">@color/arcade_bg</item>
    </style>
</resources>